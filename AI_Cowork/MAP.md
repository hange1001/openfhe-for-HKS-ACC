# 项目地图（MAP）—— 一页看懂：在哪 / 做了啥 / 写在哪 / 明天干啥

> **维护规则**：每次会话结束，AI 只更新 §1「你在这里」和 §2「下一步」，其余段落少动。
> 文件别名（推导v1 / 对表 / 符号表…）↔ 路径的对照表在 `task.yaml` 顶部 `doc_refs`。

---

## §1 你在这里（2026-09-04）

**最新工程更新（宽接口 / 去拷贝，验证完成但6ns未闭合）**：计算仍为4路，三个AXI实际256位；
移除17次独立整塔拷贝与flat中转。OpenFHE/ASan各470checks通过，真实OpenFHE RTL
40960元素通过；两digit 268170→139734周期（6ns假设下0.838404ms，非上板测量）。
综合BRAM708/DSP1160/FF78528/LUT169991/URAM96。INIT退化为295063周期须单列。
扩展RTL smoke通过（35次调用、4种digit及端口回归）；OOC 239992条网络全部布通。
6ns加0.75ns裕量WNS=-0.779ns；同布线7ns情景WNS=+0.221ns，暖态换算0.978138ms。
最差路径在AUTO的BRAM选通/取负/写回，约73%延迟来自布线；外部I/O和平台时钟未签核；未提交。
本轮细节与试错已写[日志](log.md)，证据见[宽接口报告](../docs/reports/hls/hks_wide256_direct_20260904/README.md)。
报告正文已按要求改为中文；§六澄清 AUTO 是独立旋转算子，不在 OP_HKS_DIGIT 内部，
但仍参与通用 Top 的时序检查。原始工具证据保持原文，本次没有改动硬件。
下段为此前共享版里程碑；以下学习路线保持原样。

**本轮工程进展**：已按用户要求先保存 Git 检查点 `c073b11`，再把独立 NTT/INTT
与 HKS 统一为一套双向引擎（RTL 确认 4 路模乘共享）。综合 DSP 2088→1392、
BRAM 896→704、LUT 278242→175140；URAM 96 不变。共享版暂未提交。
`OP_HKS_DIGIT` 无板卡 PoC：
INTT → QHatInv 预缩放 → BConv → NTT，保留原 EVAL digit，结果一次回写。
native / ASan / Vitis C-sim 通过（Top 18/18、HKS 22 cases）；已完成同源码基线与融合综合。
已接入 OpenFHE HYBRID：共享版 C-model 集成 470 项检查 / 1523712 个模数元素精确对比通过，
EvalRotate 两 digit 融合且解密正确；EvalMult 降为 Q=2 时正确回退 CPU。XRT 入口仅编译检查。
全库 AddressSanitizer（含泄漏检查）通过；旧全量 PKE 测试用例加载抛出 stoul 异常，未计入通过项。
共享版 Vitis C-sim、CG 11/11 和 RTL smoke co-sim（24 次调用、3 种 digit）通过。
时序未闭合（新综合估计 slack -0.291ns）；XRT 运行、P&R 和上板未做。
接口见 [HKS_DIGIT](../src/fpga_backend/HKS_DIGIT.md)，证据见
[综合报告](../docs/reports/hls/hks_digit_poc_20260904/README.md) 与
[OpenFHE 接入报告](../docs/reports/hls/hks_digit_openfhe_20260904/README.md)、
[共享引擎报告](../docs/reports/hls/hks_shared_transform_20260904/README.md)。以下学习主线保留，未代用户完成练习。

```
phase1 ██████░░░░  step 1.1 ✅ → 【step 1.3 进行中】 → 1.5/1.6 → 1.2 → 1.4
材料   D1–D2 ✅ → D3 三份【已读完】→ 【题已出，等你交图】
```

**当前动作**：做 task.yaml step 1.3 的 `exercise_roofline`（四步：定义 B0–B4 → 重推 AI →
画三层 roofline → 三道验收题）。给定值在同 step 的 `givens_for_roofline`。**你画，我改。**

**08-26 核查结果，会改变这道题的做法**：给定值 `B0/B1/B4 = 0.32/0.89/2.1` 是**无出处数字**——
标的来源「推导v1 §四」是失效引用（该节是「口径对账」，全文无此三数）；四处引用互指、无一处含推导。
且 **B2/B3 两个边界从未被定义过**。→ 第 0 步不是画图，是把 B0–B4 定义出来并重推 AI。
同类：机器平衡点 0.22 / 25.6 也只有结论没有推导。

**08-14 闭卷结果**：A 3/5、B 2/8（上次 quiz 0.5/5）。概念已通，卡在两个习惯——
**算完不复核**、**跨层数字直接相除**（B0 得 0，正是本项目历次翻车的同一根源）。详见 log 08-14。
→ 这两条已作为强制项写进本次练习（第 1 步的【复核】、第 2 步假设 (b)）。

**待你处理**：F1 (MICRO'21) PDF 不在 Zotero（D7 的 ★）；步 1.2 实验 A–D 需 U55C 板卡；
`AI_Cowork/scripts/sync-to-ob.sh` 里 PROJECT_DIR / OB_VAULT 两个路径是旧机器的，同步前先改

---

## §2 下一步（线性队列，从上往下照做）

**工程验证已收尾**：功能/RTL通过，OOC已布通，原6ns目标未闭合；日志和报告已回填。
下一轮若继续保持并行度，优先检查AUTO关键路径的流水边界/共享缓存扇出，并优化INIT退化。
7ns加0.75ns裕量只通过了内部时序情景；板级端到端性能仍需实体设备。

**复合算子工程后续（待选）**：共享 NTT 已完成；剩余 BConv 占 870 DSP 且两份
局部缓冲尚未合并。真实OpenFHE RTL对拍已完成；后续可选：审阅提交优化版、时序闭合、Q=2降层支持。
OpenFHE negacyclic 语义已用其真实单位根和变换结果验证。
已有学习队列如下，状态未改。

1. [x] ~~闭卷热身推导~~ **08-14 完成，A 3/5 / B 2/8**（题在 task.yaml 步 1.4 `warmup_exercise`）
       → 两条待改的习惯：**算完必复核**（用自己写的公式反验）、**跨层数字先问口径再相除**
2. [x] ~~**D3 读三份**~~ **08-26 读完**：Roofline (CACM'09) / TPU (ISCA'17) 的 roofline 节 /
       CS149《Locality, Communication and Contention》
3. [ ] **← 你在这**　做 `exercise_roofline`（题在 task.yaml 搜 `id: 1.3`，给定值在同 step 的 `givens_for_roofline`）
       第 0 步 定义 B0–B4（含从未定义过的 B2/B3），n_call 与 V 写符号式
       第 1 步 重推 AI，每个数标 AI_pcie / AI_axi，**算完必须反验**
       第 2 步 画三层 roofline（PCIe/HBM/AXI + 算力屋顶），先写下三条假设再画
       第 3 步 三道验收题（题眼是 Q2：0.22 与 0.23 差 4% 是不是巧合）
       → 交我批改；过了我再出正式图脚本
4. [ ] **D4–D5**：Timeloop + MAESTRO + CS149《Work Distribution》→ 把 B0–B4 表填成**符号式**（= step 1.3 主交付物）+ 负载均衡符号式
5. [ ] **D7**：F1（先弄到 PDF）+ 扫 BTS/ARK → 画系统框图，**边界线含 host↔加速器**（= step 1.3 交付物之三）
6. [ ] **step 1.5**（0.5 天）：跑 test-cg-ntt 验证 bit-reversal 归宿
7. [ ] **step 1.6**（0.5 天）：工业参数纸面预算（含 N 维 tiling 问题）
8. [ ] **step 1.2**（需板卡）：实验 A/B/C/D，统一墙钟口径
9. [ ] **step 1.4**：三模型 v1，串行版 + 重叠版并列
10. → phase2（P&R 时序闭环）。D6 (pp4fpgas) 到这里才读

---

## §3 已完成的事（按时间，产出可点开）

| 日期 | 做了什么 | 写在哪 |
|---|---|---|
| 06-29 | AI_Cowork 初始化；OC vs CiFlow gap 分析 | gap分析 |
| 08-03 | L1/L2 建模方法论框架 | 建模指南 |
| 08-04 | **L1/L2 推导 v1**：算子账五点表、三模型、8.0/4.5ms 口径对账 | 推导v1 |
| 08-04 | 读 HERA/CiFlow 原文；twiddle 两套架构核查；task.yaml 改组四阶段 | task.yaml q6/q7 |
| 08-04→07 | **step 1.1 L0 闭卷手推**（BConv 误差界 / ModUp 目标基 / ModDown） | 闭卷推导、手推、符号表 |
| 08-11 | **Step A**：导师 8 条清单 ↔ 项目对表，圈出 3 个真空白 | 对表 |
| 08-13 | **D1–D2 收尾**：查实 offload 全阻塞、延迟数字是【下界】、超乘性、新候选步 3.4 | 对表 §3.1、task.yaml q8 |
| 08-13 | quiz 0.5/5 → 诊断「AI 推导用户旁观」→ 角色翻转 | 日志 08-13·续 |
| 08-13 | doc_refs 引用锚点表（15 条路径全验证）+ 本地图 | task.yaml 顶部 |

---

## §4 核心结论速查（一头雾水时先读这五条）

1. **三策略延迟差必然 <5%**：每算子一次 offload 下 AI 恒等——排序是平台单价的性质，不是策略的性质 → 推导v1 §2.3/§5.1
2. **修 OC 天花板 8%；OC+片上驻留 29%**：收益 90% 来自驻留 → 推导v1 §3.4
3. **offload 完全阻塞，182.8 μs/call 是【下界】不是墙钟**（漏计 bo 分配 + memcpy）→ task.yaml q8
4. **微架构优化 × 传输重叠是超乘性的**（1.69×·1.49× 一起做 = 3.06× > 2.52×），排期必须捆绑；比值只能汇报不能运算 → 对表 §3.1
5. **瓶颈随系统边界变**：HERA 的图没有 PCIe 屋顶所以瓶颈是 NTT；画上 PCIe 屋顶才是本项目的 Fig.1 → 对表 §3.3

---

## §5 文件地图（想知道什么 → 打开哪个）

| 你想知道… | 打开 |
|---|---|
| 今天干什么 | **本文件 §2** |
| 某任务的验收标准 / 全部待办 | task.yaml（搜 `id: 1.3` 这样定位） |
| 某个术语什么意思 / 学习材料清单 | 对表（§1 术语 / §5 材料） |
| 符号、参数、AI 口径规定 | 符号表（§9.1 = AI 三规定） |
| 为什么三策略只差 5%、模型长什么样 | 推导v1 |
| HKS 数学怎么推的 | 闭卷推导（过程与踩坑在 手推） |
| 之前每次会话干了啥 | 日志（log.md，倒序） |
| 某架构决策当时为什么这么定 | ADR（decisions.md） |
| 论文 PDF / Portfolio 在哪 | task.yaml `doc_refs.external` |
| 项目背景（给 AI 冷启动） | README.md |

---

## §6 task.yaml 的正确用法

**它是数据库，不是阅读材料。** 按 phase 归档（1.1→1.6, 2.x, 3.x, 4.x），**不按执行顺序**——
真实执行序是 `1.3 → 1.5/1.6 → 1.2 → 1.4`（理由见其 step 1.3 `order_note`：模型结构决定测什么，所以先建模后测量）。

- 想知道「现在做什么」→ 看本文件 §2，别通读 task.yaml
- 想查某个 step 的细节 → 编辑器里搜 `id: 1.3`
- 想查某个悬而未决的问题 → 搜 `open_questions`（q1–q8，答过的带 `status: answered`）
- 三条铁律在 `global_validation`：数字要有出处 / 先预测后实测 / 延迟数字全是下界
