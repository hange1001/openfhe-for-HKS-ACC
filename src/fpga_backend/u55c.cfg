part=xcu55c-fsvh2892-2L-e

[connectivity]
# 单 Top kernel 设计，所有操作通过 opcode 分发
nk=Top:1:Top_1

# AXI 端口到 DDR bank 映射（与 top.cpp 中的 HLS INTERFACE bundle 对应）
# mem_in1, mem_out → gmem0 → DDR[0]
# mem_in2          → gmem1 → DDR[1]
sp=Top_1.mem_in1:DDR[0]
sp=Top_1.mem_in2:DDR[1]
sp=Top_1.mem_out:DDR[2]
