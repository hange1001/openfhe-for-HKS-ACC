#include "../include/bconv_naive.h"

Compute_BConv_Naive(
    uint64_t in_x[MAX_LIMBS][SQRT][SQRT],
    const uint16_t in_w[LIMB_Q][MAX_OUT_COLS],
    const uint16_t out_mod[MAX_OUT_COLS],
    int sizeP
){
    



}